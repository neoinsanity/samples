"""The setuptools setup file.

"""

from setuptools import find_packages, setup

setup(
    name='ook_vedis',
    version='0.0.0.x',
    license='Apache License 2.0',
    description='A need to remember.',
    packages=['ook-vedis',],
    install_requires=[],
    include_package_data = True,
)
